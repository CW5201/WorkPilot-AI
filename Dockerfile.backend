FROM python:3.11-slim

WORKDIR /opt/app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY app ./app
COPY data ./data

ENV PYTHONPATH=/opt/app
EXPOSE 8000

# 首次启动：如配置了 Postgres 则建表；否则本地 JSON 持久化即可
CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]
